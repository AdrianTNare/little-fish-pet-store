{
  "name": "little-fish-pet-store",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky",
    "dev": "rm -f ./apps/backend/bin/Debug/net9.0/package.json && pnpm -r dev",
    "lint": "pnpm -r lint",
    "build": "rm -f ./apps/backend/bin/Debug/net9.0/package.json && pnpm -r build",
    "test": "pnpm -r test",
    "start": "pnpm -r start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0"
  }
}
